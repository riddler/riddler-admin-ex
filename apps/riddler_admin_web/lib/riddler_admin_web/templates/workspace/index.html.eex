<h1>Listing Workspaces</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for workspace <- @workspaces do %>
    <tr style="<%= if workspace.id == @current_workspace_id, do: "font-weight: bold" %>">
      <td><%= workspace.name %></td>

      <td>
        <span><%= link "Show", to: Routes.account_workspace_path(@conn, :show, @account_id, workspace) %></span>
        <span><%= link "Switch", to: Routes.account_workspace_workspace_switch_path(@conn, :switch, @account_id, workspace.id) %></span>
        <span><%= link "Edit", to: Routes.account_workspace_path(@conn, :edit, @account_id, workspace) %></span>
        <span><%= link "Delete", to: Routes.account_workspace_path(@conn, :delete, @account_id, workspace), method: :delete, data: [confirm: "Are you sure?"] %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Workspace", to: Routes.account_workspace_path(@conn, :new, @account_id) %></span>
