<h1>New Agent</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.workspace_agent_path(@conn, :create, @workspace_id)) %>

<span><%= link "Back", to: Routes.workspace_agent_path(@conn, :index, @workspace_id) %></span>
