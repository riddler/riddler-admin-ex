<h1>Listing Environments</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Key</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for environment <- @environments do %>
    <tr>
      <td><%= environment.name %></td>
      <td><%= environment.key %></td>

      <td>
        <span><%= link "Show", to: Routes.workspace_environment_path(@conn, :show, @workspace_id, environment) %></span>
        <span><%= link "Edit", to: Routes.workspace_environment_path(@conn, :edit, @workspace_id, environment) %></span>
        <span><%= link "Delete", to: Routes.workspace_environment_path(@conn, :delete, @workspace_id, environment), method: :delete, data: [confirm: "Are you sure?"] %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Environment", to: Routes.workspace_environment_path(@conn, :new, @workspace_id) %></span>
<span><%= link "Back", to: Routes.workspace_path(@conn, :show, @workspace_id) %></span>
