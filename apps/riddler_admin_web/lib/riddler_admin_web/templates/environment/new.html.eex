<h1>New Environment</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.workspace_environment_path(@conn, :create, @workspace_id)) %>

<span><%= link "Back", to: Routes.workspace_environment_path(@conn, :index, @workspace_id) %></span>
