<h1>New Condition</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.workspace_condition_path(@conn, :create, @workspace_id)) %>

<span><%= link "Back", to: Routes.workspace_condition_path(@conn, :index, @workspace_id) %></span>
