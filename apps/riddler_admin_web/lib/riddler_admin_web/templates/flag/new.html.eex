<h1>New Flag</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.workspace_flag_path(@conn, :create, @workspace_id)) %>

<span><%= link "Back", to: Routes.workspace_flag_path(@conn, :index, @workspace_id) %></span>
