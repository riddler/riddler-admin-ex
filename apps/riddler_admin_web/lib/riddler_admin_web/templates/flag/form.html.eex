<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <%= label f, :key %>
  <%= text_input f, :key %>
  <%= error_tag f, :key %>

  <%= label f, :include_source %>
  <%= textarea f, :include_source %>
  <%= error_tag f, :include_source %>

  <div>
    <%= submit "Save" %>
  </div>
<% end %>
